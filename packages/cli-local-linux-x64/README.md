# @second-inc/cli-local-linux-x64

Platform payload for `@second-inc/cli` on Linux x64, including Windows via WSL2.

This package is installed automatically by the Second CLI. Most users should not install it directly.

Run Second with:

```bash
npx --yes @second-inc/cli
```

Docs: https://docs.second.so
