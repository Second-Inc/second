# @second-inc/cli-local-darwin-x64

Platform payload for the Second local runtime on Intel Macs.

This package is installed by `@second-inc/cli` and bundled into the desktop
installer. Users should not install it directly.
