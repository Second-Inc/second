# @second-inc/cli-local-win32-x64

Windows payload for `@second-inc/cli`.

This package launches the Linux local runtime inside WSL2 so Windows users can run:

```powershell
npx --yes @second-inc/cli
```
